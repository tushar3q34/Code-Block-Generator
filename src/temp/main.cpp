#include <bits/stdc++.h>

using namespace std;

void solve(){
    int l;
    cin >> l;
    int r;
    cin >> r;

}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int t;
    cin >> t;
    while (t--){
        solve();
    }
}