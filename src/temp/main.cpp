#include <bits/stdc++.h>

using namespace std;

void solve(){
    int n;
    cin >> n;
    int separated[][n];
    for (int i = 0; i < ; i++){
        for (int j = 0; j < n; j++){
            cin >> separated[i][j];
        }
    }
    int separated[n][n-1];
    for (int i = 0; i < n; i++){
        for (int j = 0; j < n-1; j++){
            cin >> separated[i][j];
        }
    }
    
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int t;
    cin >> t;
    while (t--){
        solve();
    }
}